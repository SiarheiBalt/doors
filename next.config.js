/** @type {import('next').NextConfig} */
const nextConfig = {
    /*
    images: {
        remotePatterns: [
            {
                protocol: 'https',
                hostname: 'profildoors.ru',
                // port: '',
                pathname: '/images/**',
                // search: '',
            },
        ],
    },
     */
}

module.exports = nextConfig
