import {cl} from "../classes/global";

export default function Home() {

    return (
        <div className={cl.container}>
            <div className="pt-10">
                <span className={cl.title}>Home page</span>
            </div>
        </div>
    )
}
