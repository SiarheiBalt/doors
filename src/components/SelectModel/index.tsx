import React from 'react';

const SelectModel = () => {

    return (
        <div className={styles.container}>

        </div>
    );
};

export default SelectModel;

const styles = {
    container: ""
}